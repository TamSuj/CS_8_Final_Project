//
// Created by Tammy S on 11/18/23.
//

#include "File.h"

void File::push_back(std::vector<std::string> list) {
//    auto newFile = fileDropdown(list, pos, {100, 50});
//    fileList.push_back(&newFile);

}

File::File(const std::string &filepath) {
    //Pretty much take in file path where list is iterate through and once the keyword "Folder" is found
    // Call push_back and pass in the list of files in that folder
//    std::string temp = "";
//    std::string list = "Folder1/NameList/myWords.txt";
//
//    for (int i = 0; i < list.size(); ++i) {
//        if(list[i] != '/' && list[i] != '.') {
//            temp += list[i];
//        }
//        else if(list[i] == '/') {
//            std::cout << temp << std::endl;
//            temp = "";
//        }
//
//
//    }

}
